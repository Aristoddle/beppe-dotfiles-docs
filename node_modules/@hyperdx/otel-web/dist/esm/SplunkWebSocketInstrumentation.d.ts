import { InstrumentationBase, InstrumentationConfig } from '@opentelemetry/instrumentation';
interface SplunkWebSocketInstrumentationConfig extends InstrumentationConfig {
    ignoreUrls?: (string | RegExp)[];
}
export declare class SplunkWebSocketInstrumentation extends InstrumentationBase {
    listener2ws2patched: WeakMap<object, any>;
    protected _config: SplunkWebSocketInstrumentationConfig;
    constructor(config: SplunkWebSocketInstrumentationConfig);
    init(): void;
    enable(): void;
    disable(): void;
    private startSpan;
    private patchSend;
    private addPatchedListener;
    private removePatchedListener;
    private patchEventListener;
    private endSpanExceptionally;
}
export {};
