import { BatchSpanProcessor, BatchSpanProcessorBrowserConfig, SpanExporter } from '@opentelemetry/sdk-trace-base';
export declare class HyperDXBatchSpanProcessor extends BatchSpanProcessor {
    private _visibilityChangeListener?;
    private _pageHideListener?;
    constructor(_exporter: SpanExporter, config?: BatchSpanProcessorBrowserConfig);
    private onInit;
}
