"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.hyperdxIntegration = exports._hyperdxIntegration = void 0;
const core_1 = require("@sentry/core");
const INTEGRATION_NAME = 'HyperDX';
exports._hyperdxIntegration = ((options = {}) => {
    return {
        name: INTEGRATION_NAME,
        processEvent(event) {
            var _a, _b, _c, _d, _e, _f, _g, _h;
            const possibleEventMessages = _getPossibleEventMessages(event);
            if (possibleEventMessages.length > 0) {
                event.message = possibleEventMessages[possibleEventMessages.length - 1];
            }
            const exceptions = (_a = event.exception) === null || _a === void 0 ? void 0 : _a.values;
            if (exceptions && exceptions.length > 0) {
                for (const exception of exceptions) {
                    if ((_b = exception.stacktrace) === null || _b === void 0 ? void 0 : _b.frames) {
                        const _filteredFrames = [];
                        let shouldRemoveNextFrameIfSameFile = null;
                        for (let i = exception.stacktrace.frames.length - 1; i >= 0; i--) {
                            const frame = exception.stacktrace.frames[i];
                            if ((_c = frame.filename) === null || _c === void 0 ? void 0 : _c.includes('framework-')) {
                                continue;
                            }
                            else if (((_d = frame.function) === null || _d === void 0 ? void 0 : _d.endsWith('hdxReportString')) ||
                                ((_e = frame.function) === null || _e === void 0 ? void 0 : _e.endsWith('hdxReportError')) ||
                                ((_f = frame.function) === null || _f === void 0 ? void 0 : _f.endsWith('hdxReportErrorEvent')) ||
                                ((_g = frame.function) === null || _g === void 0 ? void 0 : _g.endsWith('hdxReportEvent'))) {
                                continue;
                            }
                            else if ((_h = frame.function) === null || _h === void 0 ? void 0 : _h.endsWith('hdxReport')) {
                                shouldRemoveNextFrameIfSameFile = frame.filename;
                                continue;
                            }
                            else if (shouldRemoveNextFrameIfSameFile &&
                                frame.filename === shouldRemoveNextFrameIfSameFile) {
                                shouldRemoveNextFrameIfSameFile = null;
                                continue;
                            }
                            _filteredFrames.unshift(frame);
                        }
                        exception.stacktrace.frames = _filteredFrames;
                    }
                }
            }
            return event;
        },
    };
});
exports.hyperdxIntegration = (0, core_1.defineIntegration)(exports._hyperdxIntegration);
function _getPossibleEventMessages(event) {
    const possibleMessages = [];
    if (event.message) {
        possibleMessages.push(event.message);
    }
    let lastException;
    try {
        lastException = event.exception.values[event.exception.values.length - 1];
    }
    catch (e) {
    }
    if (lastException) {
        if (lastException.value) {
            possibleMessages.push(lastException.value);
            if (lastException.type) {
                possibleMessages.push(`${lastException.type}: ${lastException.value}`);
            }
        }
    }
    return possibleMessages;
}
