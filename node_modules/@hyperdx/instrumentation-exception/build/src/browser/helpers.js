"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.wrap = exports.ignoreNextOnError = exports.shouldIgnoreOnError = exports.WINDOW = void 0;
const core_1 = require("@sentry/core");
const utils_1 = require("@sentry/utils");
exports.WINDOW = utils_1.GLOBAL_OBJ;
let ignoreOnError = 0;
function shouldIgnoreOnError() {
    return ignoreOnError > 0;
}
exports.shouldIgnoreOnError = shouldIgnoreOnError;
function ignoreNextOnError() {
    ignoreOnError++;
    setTimeout(() => {
        ignoreOnError--;
    });
}
exports.ignoreNextOnError = ignoreNextOnError;
function wrap(fn, options = {}, before) {
    if (typeof fn !== 'function') {
        return fn;
    }
    try {
        const wrapper = fn.__sentry_wrapped__;
        if (wrapper) {
            return wrapper;
        }
        if ((0, utils_1.getOriginalFunction)(fn)) {
            return fn;
        }
    }
    catch (e) {
        return fn;
    }
    const sentryWrapped = function () {
        const args = Array.prototype.slice.call(arguments);
        try {
            if (before && typeof before === 'function') {
                before.apply(this, arguments);
            }
            const wrappedArguments = args.map((arg) => wrap(arg, options));
            return fn.apply(this, wrappedArguments);
        }
        catch (ex) {
            ignoreNextOnError();
            (0, core_1.withScope)((scope) => {
                scope.addEventProcessor((event) => {
                    if (options.mechanism) {
                        (0, utils_1.addExceptionTypeValue)(event, undefined, undefined);
                        (0, utils_1.addExceptionMechanism)(event, options.mechanism);
                    }
                    event.extra = {
                        ...event.extra,
                        arguments: args,
                    };
                    return event;
                });
                (0, core_1.captureException)(ex);
            });
            throw ex;
        }
    };
    try {
        for (const property in fn) {
            if (Object.prototype.hasOwnProperty.call(fn, property)) {
                sentryWrapped[property] = fn[property];
            }
        }
    }
    catch (_oO) { }
    (0, utils_1.markFunctionWrapped)(sentryWrapped, fn);
    (0, utils_1.addNonEnumerableProperty)(fn, '__sentry_wrapped__', sentryWrapped);
    try {
        const descriptor = Object.getOwnPropertyDescriptor(sentryWrapped, 'name');
        if (descriptor.configurable) {
            Object.defineProperty(sentryWrapped, 'name', {
                get() {
                    return fn.name;
                },
            });
        }
    }
    catch (_oO) { }
    return sentryWrapped;
}
exports.wrap = wrap;
