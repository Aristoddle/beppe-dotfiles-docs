"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.modulesIntegration = exports._modulesIntegration = void 0;
const node_fs_1 = require("node:fs");
const node_path_1 = require("node:path");
const core_1 = require("@sentry/core");
let moduleCache;
const INTEGRATION_NAME = 'Modules';
exports._modulesIntegration = (() => {
    return {
        name: INTEGRATION_NAME,
        processEvent(event) {
            event.modules = {
                ...event.modules,
                ..._getModules(),
            };
            return event;
        },
    };
});
exports.modulesIntegration = (0, core_1.defineIntegration)(exports._modulesIntegration);
function getPaths() {
    try {
        return require.cache
            ? Object.keys(require.cache)
            : [];
    }
    catch (e) {
        return [];
    }
}
function collectModules() {
    const mainPaths = (require.main && require.main.paths) || [];
    const paths = getPaths();
    const infos = {};
    const seen = {};
    paths.forEach((path) => {
        let dir = path;
        const updir = () => {
            const orig = dir;
            dir = (0, node_path_1.dirname)(orig);
            if (!dir || orig === dir || seen[orig]) {
                return undefined;
            }
            if (mainPaths.indexOf(dir) < 0) {
                return updir();
            }
            const pkgfile = (0, node_path_1.join)(orig, 'package.json');
            seen[orig] = true;
            if (!(0, node_fs_1.existsSync)(pkgfile)) {
                return updir();
            }
            try {
                const info = JSON.parse((0, node_fs_1.readFileSync)(pkgfile, 'utf8'));
                infos[info.name] = info.version;
            }
            catch (_oO) {
            }
        };
        updir();
    });
    return infos;
}
function _getModules() {
    if (!moduleCache) {
        moduleCache = collectModules();
    }
    return moduleCache;
}
