"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.hashFromStack = exports.hashFrames = exports.functionNamesMatch = exports.isAnonymous = exports.createRateLimiter = void 0;
function createRateLimiter(maxPerSecond, enable, disable) {
    let count = 0;
    let retrySeconds = 5;
    let disabledTimeout = 0;
    setInterval(() => {
        if (disabledTimeout === 0) {
            if (count > maxPerSecond) {
                retrySeconds *= 2;
                disable(retrySeconds);
                if (retrySeconds > 86400) {
                    retrySeconds = 86400;
                }
                disabledTimeout = retrySeconds;
            }
        }
        else {
            disabledTimeout -= 1;
            if (disabledTimeout === 0) {
                enable();
            }
        }
        count = 0;
    }, 1000).unref();
    return () => {
        count += 1;
    };
}
exports.createRateLimiter = createRateLimiter;
function isAnonymous(name) {
    return (name !== undefined &&
        (name.length === 0 || name === '?' || name === '<anonymous>'));
}
exports.isAnonymous = isAnonymous;
function functionNamesMatch(a, b) {
    return a === b || (isAnonymous(a) && isAnonymous(b));
}
exports.functionNamesMatch = functionNamesMatch;
function hashFrames(frames) {
    if (frames === undefined) {
        return;
    }
    return frames
        .slice(-10)
        .reduce((acc, frame) => `${acc},${frame.function},${frame.lineno},${frame.colno}`, '');
}
exports.hashFrames = hashFrames;
function hashFromStack(stackParser, stack) {
    if (stack === undefined) {
        return undefined;
    }
    return hashFrames(stackParser(stack, 1));
}
exports.hashFromStack = hashFromStack;
