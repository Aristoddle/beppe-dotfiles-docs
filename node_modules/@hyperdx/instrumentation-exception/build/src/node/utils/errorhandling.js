"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.logAndExitProcess = void 0;
const utils_1 = require("@sentry/utils");
const api_1 = require("@opentelemetry/api");
const DEFAULT_SHUTDOWN_TIMEOUT = 2000;
class TimeoutError extends Error {
    constructor() {
        super('Timeout');
        this.name = 'TimeoutError';
    }
}
const timeoutPromise = (timeout) => {
    return new Promise((resolve, reject) => {
        setTimeout(() => {
            reject(new TimeoutError());
        }, timeout);
    });
};
async function logAndExitProcess(error, forceFlush) {
    (0, utils_1.consoleSandbox)(() => {
        console.error(error);
    });
    try {
        await Promise.race([
            forceFlush(),
            timeoutPromise(DEFAULT_SHUTDOWN_TIMEOUT),
        ]);
        global.process.exit(1);
    }
    catch (error) {
        if (error instanceof TimeoutError) {
            api_1.diag.error('Timeout while flushing events, forcing exit');
        }
        api_1.diag.error('Error while flushing events', error);
        global.process.exit(1);
    }
}
exports.logAndExitProcess = logAndExitProcess;
