import type { JsonObject, JsonValue } from 'type-fest';
export interface Log {
    body?: JsonValue;
    timeUnixNano: number;
    attributes?: JsonObject;
}
export interface LogExporter {
    export(spans: Log[]): void;
}
