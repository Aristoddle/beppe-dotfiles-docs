/// <reference types="node" />
import type { JsonValue, JsonObject } from 'type-fest';
import type { Log, LogExporter } from './types';
export interface BatchLogProcessorConfig {
    scheduledDelayMillis?: number;
}
export declare class BatchLogProcessor {
    private logs;
    scheduledDelayMillis: number;
    timeout: NodeJS.Timeout | undefined;
    exporter: LogExporter;
    lastBatchSent: number;
    constructor(exporter: LogExporter, config: BatchLogProcessorConfig);
    onLog(log: Log): void;
    _flushAll(): void;
}
export declare function convert(body: JsonValue, timeUnixNano: number, attributes?: JsonObject): Log;
